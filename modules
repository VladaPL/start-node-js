// * Чтобы использовать модуль применяем require и указываем путь подключаемого модуля.
// Деструктурируем объект module.exports, чтобы получить переменную user.

// const {userName: user, sayHi} = require('./test');

// const name = 'Anna';
// console.log(sayHi(name));


// * Подключение сторонних библиотек
// В node есть встроенные модули,
// например os (помогает получить разные сведения об операционной системе пользователя).
// const os = require('os');
// Обратимся к методу, который выведет данные о платформе и ее релизе.
// console.log(os.platform(), os.release());

